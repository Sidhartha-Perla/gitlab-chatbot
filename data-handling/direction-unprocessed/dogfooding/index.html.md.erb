---
layout: markdown_page
title: "Dogfooding"
description: "Dogfooding status page"
canonical_path: "/direction/dogfooding/"
---

## Legend

| Status | Description |
| ----- | ----- |
| `N/A` | Category is not something we need at GitLab |
| `Planned` | We plan to use this category |
| `Active` | Category is being used |
| `Not Planned` | We don't have plans to use this category |

## Category Dogfooding Status

| Stage | Group | Category | Status |
| ----- | ----- | ----- | ----- | ----- |----- | 
<% data.stages.stages.each do |stageKey, stage| %>
  <% if stage && stage.groups %>
    <% stage.groups.each do |groupKey, group| %>
      <% if group && group.categories %>
        <% group.categories.each do |categoryKey| %>
          <% next if categoryKey == 'rTBD' %>
          <% category = data.categories[categoryKey] %>
          <% next unless category %>
          <% if category.marketing %>
            | <%= stage.display_name %> | <%= group.name %> | <%= category.name %> | <%= category.dogfooding_status %> |
          <% end %>
        <% end %>
      <% end %>
    <% end %>
  <% end %>
<% end %>
